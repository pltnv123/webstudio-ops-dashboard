# Motion System v32

Hero reveal, trust card stagger, consultation path steps, certificate reveal, CTA hover, mobile-safe micro interactions and reduced-motion fallback. HyperFrames is the production export lane; MP4 requires render gate.
