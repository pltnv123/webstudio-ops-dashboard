# UI Implementation v3.0

Files changed: `src/index.html`, `src/app.js`, `src/styles.css`, `scripts/build_snapshot.py`, `scripts/smoke_check.py`.

Added static route `/order-builder/`, nav tab, `orderBuilder()` renderer, demo snapshot ingestion, and smoke checks.
