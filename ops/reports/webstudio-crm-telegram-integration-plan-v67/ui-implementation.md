# UI Implementation

Implemented static route `/integration-plan/` through `integrationPlanWorkflow()` in `src/app.js`, nav link in `src/index.html`, route directory generation in `scripts/build_snapshot.py`, and smoke markers in `scripts/smoke_check.py`.

Required visible markers: integration-plan-v67, Telegram bot intake plan, CRM / Sheets plan, Supabase live write plan, OWNER_APPROVAL_REQUIRED, DO_NOT_RUN_LIVE, READY_FOR_REVIEW.
