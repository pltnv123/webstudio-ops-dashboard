# Continuation prompt — WebStudio V2.6

Resume from `/workspace/output/webstudio-supabase-memory-ui-v26/state.json` and repo `/workspace/tmp/webstudio-ops-dashboard-pr` on branch `webstudio/product-build-v31`.

Next safe step if interrupted: inspect `git status --porcelain=v1 -uall`, run `npm run build && npm run smoke`, push only allowlisted files, verify GitHub Actions/Pages, write Supabase row, run `hfinalize`.

Safety: no secrets, no destructive Supabase changes, no officebot, no gateway/systemd restart, no force push.
