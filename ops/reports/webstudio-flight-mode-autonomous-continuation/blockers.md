# Blockers

Current blocker: none for V2.9–V3.2 deploy/status. V3.3 was skipped because report push/finalization consumed the available safe window.

Known limitation: Docker cannot read private GitHub Actions details without host GitHub auth. Deployment was verified through public Pages HTTP 200 route checks.
