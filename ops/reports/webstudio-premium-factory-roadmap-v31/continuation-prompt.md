# Continuation Prompt v3.1

Continue from repo `/workspace/tmp/webstudio-ops-dashboard-pr` and output `/workspace/output/webstudio-premium-factory-roadmap-v31`. Verify local HEAD, run build/smoke/secret scan, restore host GitHub push bridge, push branch `webstudio/product-build-v31`, verify Pages route, then update Supabase status to DEPLOYED.
