# Revision Workflow Board — v5.3

Status: IN_PROGRESS

Route: `/revision-workflow/`

Static/sanitized only. No private data and no live writes.

UI route is implemented in `src/app.js`; static snapshot data is implemented in `scripts/build_snapshot.py`.
