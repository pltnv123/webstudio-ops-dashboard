# Continuation Prompt — WebStudio V3.5

Continue from `/workspace/output/webstudio-generated-demo-website-v35` and repo `/workspace/tmp/webstudio-ops-dashboard-v35`.

If not already complete:
1. Verify `npm run build`, `npm run smoke`, local static route, and secret scan.
2. Commit/push branch `webstudio/product-build-v31`.
3. Verify GitHub Actions Pages and public route `/generated-demo-site-v35/` HTTP 200 with Northstar/demo markers.
4. Write Supabase operational rows and update `supabase-status-update.md`.
5. Run `hfinalize` before final response.

Do not use real client data, live booking/CRM/payment writes, public health claims, destructive Supabase changes, force push, or officebot.
